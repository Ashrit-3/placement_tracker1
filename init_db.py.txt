def init_db():
    with app.app_context():  # <-- ensures app context
        db.create_all()
        # Optional: create default admin user
        if not User.query.filter_by(username="admin").first():
            admin_user = User(username="admin", password="admin123", skills="", is_admin=True)
            db.session.add(admin_user)
            db.session.commit()
        print("✅ Database initialized successfully with admin user!")
